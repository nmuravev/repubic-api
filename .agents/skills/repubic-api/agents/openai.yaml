interface:
  display_name: "Repubic Api"
  short_description: "Repo-specific patterns and workflows for repubic-api"
  default_prompt: "Use the repubic-api repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true